//
//  ColorExtension.swift
//  Taskly
//
//  Created by Islam Rzayev on 17.12.24.
//

import SwiftUI

extension Color {
    // MARK: - Option 1: Professional Theme
    static let professionalBackground = Color(red: 243 / 255, green: 244 / 255, blue: 246 / 255) // Light Grey
    static let professionalTitle = Color(red: 44 / 255, green: 62 / 255, blue: 80 / 255)         // Navy Blue
    static let professionalSubtitle = Color(red: 127 / 255, green: 140 / 255, blue: 141 / 255)   // Grey
    static let professionalDateTime = Color(red: 243 / 255, green: 156 / 255, blue: 18 / 255)    // Orange
    static let professionalSeparator = Color(red: 211 / 255, green: 211 / 255, blue: 211 / 255)  // Soft Grey
    
    // MARK: - Option 2: Modern and Clean
    static let modernBackground = Color(red: 225 / 255, green: 234 / 255, blue: 242 / 255)      // Muted Blue
    static let modernTitle = Color(red: 0 / 255, green: 137 / 255, blue: 123 / 255)            // Teal
    static let modernSubtitle = Color(red: 112 / 255, green: 164 / 255, blue: 160 / 255)       // Light Teal
    static let modernDateTime = Color(red: 242 / 255, green: 201 / 255, blue: 76 / 255)        // Yellow-Gold
    static let modernSeparator = Color(red: 160 / 255, green: 164 / 255, blue: 168 / 255)      // Soft Grey
    
    // MARK: - Option 3: Dark and Minimalistic
    static let darkBackground = Color(red: 44 / 255, green: 62 / 255, blue: 80 / 255)          // Dark Navy
    static let darkTitle = Color(red: 255 / 255, green: 255 / 255, blue: 255 / 255)            // White
    static let darkSubtitle = Color(red: 189 / 255, green: 195 / 255, blue: 199 / 255)         // Light Grey
    static let darkDateTime = Color(red: 243 / 255, green: 156 / 255, blue: 18 / 255)          // Orange
    static let darkSeparator = Color(red: 79 / 255, green: 91 / 255, blue: 102 / 255)          // Muted Blue
    
    // MARK: - Option 4: Calm and Neutral
       static let calmBackground = Color(red: 253 / 255, green: 252 / 255, blue: 251 / 255) // Light cream
       static let calmTitle = Color(red: 58 / 255, green: 77 / 255, blue: 57 / 255)         // Deep green
       static let calmSubtitle = Color(red: 94 / 255, green: 111 / 255, blue: 100 / 255)   // Green-grey
       static let calmDateTime = Color(red: 255 / 255, green: 159 / 255, blue: 67 / 255)   // Orange-yellow
       static let calmSeparator = Color(red: 221 / 255, green: 221 / 255, blue: 221 / 255) // Light grey

       // MARK: - Option 5: Bold and Vibrant
       static let vibrantBackground = Color(red: 30 / 255, green: 30 / 255, blue: 46 / 255)  // Dark Indigo
       static let vibrantTitle = Color(red: 224 / 255, green: 175 / 255, blue: 255 / 255)    // Light purple
       static let vibrantSubtitle = Color(red: 195 / 255, green: 186 / 255, blue: 198 / 255) // Muted greyish-pink
       static let vibrantDateTime = Color(red: 255 / 255, green: 83 / 255, blue: 112 / 255)  // Vibrant pink
       static let vibrantSeparator = Color(red: 73 / 255, green: 77 / 255, blue: 91 / 255)   // Dark separator

       // MARK: - Option 6: Warm and Earthy
       static let earthyBackground = Color(red: 255 / 255, green: 247 / 255, blue: 227 / 255) // Warm beige
       static let earthyTitle = Color(red: 109 / 255, green: 76 / 255, blue: 65 / 255)        // Rich brown
       static let earthySubtitle = Color(red: 141 / 255, green: 110 / 255, blue: 99 / 255)    // Softer brown
       static let earthyDateTime = Color(red: 255 / 255, green: 112 / 255, blue: 67 / 255)    // Soft orange
       static let earthySeparator = Color(red: 188 / 255, green: 170 / 255, blue: 164 / 255)  // Light taupe
   }
